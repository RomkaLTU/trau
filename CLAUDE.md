# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

See [AGENTS.md](AGENTS.md) for all guidance: build/test commands, architecture, conventions, and invariants.

## Agent skills

### Issue tracker

Issues and PRDs live in Linear under team `COD` and project `trau`. See `docs/agents/issue-tracker.md`.

### Triage labels

Linear uses the canonical triage roles, with `needs-human` for human-required work. See `docs/agents/triage-labels.md`.

### Domain docs

This is a single-context repo with root `CONTEXT.md` and `docs/adr/`. See `docs/agents/domain.md`.
