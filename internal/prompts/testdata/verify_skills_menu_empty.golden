Load these required skills with the Skill tool before verifying: tdd. Do NOT pause to ask which skills to load. Pass each name to the Skill tool exactly as written — the names contain no spaces; if a Skill call returns "Unknown skill", re-call it with the name copied exactly from this list rather than proceeding without it.