This is an unattended run — load these required skills with the Skill tool before implementing: beta. Do NOT pause to ask which skills to load. Other installed skills are available — load any that genuinely help: alpha, gamma. Pass each name to the Skill tool exactly as written — the names contain no spaces; if a Skill call returns "Unknown skill", re-call it with the name copied exactly from this list rather than proceeding without it.