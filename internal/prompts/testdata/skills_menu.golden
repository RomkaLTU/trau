This is an unattended run — load these required skills with the Skill tool before implementing: beta. Do NOT pause to ask which skills to load. Other installed skills are available — load any that genuinely help: alpha, gamma.