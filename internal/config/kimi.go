package config

// KimiDefaultMode drives the kimi TUI in a terminal session, which is what makes a
// kimi phase steerable mid-run. The `kimi -p` print mode remains available as a
// fallback while the interactive path soaks.
const KimiDefaultMode = "interactive"
